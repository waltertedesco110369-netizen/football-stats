services:
  - type: web
    name: football-stats-mobile
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app_mobile.py --server.port $PORT --server.address 0.0.0.0
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: PORT
        value: 8505
    healthCheckPath: /

